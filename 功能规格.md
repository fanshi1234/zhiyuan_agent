# 志愿Agent — 功能规格文档 (Clean-Room Spec)

## 1. 项目概述

一个基于本地 Web 服务器的高考志愿填报 AI 助手。用户通过浏览器与系统对话，系统结合本地录取数据库和联网搜索结果，通过 LLM 生成个性化的志愿填报建议。

## 2. 核心功能模块

### 2.1 聊天界面 (前端)
- **布局**: 左侧对话列表，右侧消息区域
- **双模式**: 报考模式（严肃志愿分析）和娱乐模式（幽默吐槽风格）
- **多对话管理**: 支持创建/删除/切换多个对话会话
- **流式响应**: 实时显示 LLM 生成的文字
- **本地存储**: 所有对话历史和设置保存在浏览器 localStorage
- **深色模式**: 一键切换亮色/暗色主题

### 2.2 用户输入处理 (前端 + 后端)
- 用户输入自然语言描述高考情况（省份、分数、位次、专业偏好等）
- **两步提取策略**:
  1. 首先调用 LLM 进行语义理解（处理"一万三"、"物化生"等口语表达）
  2. 正则表达式兜底（提取省份、分数、位次、学校名、专业关键词）
- **提取字段**: 省份、总分、全省排名、科目类别、目标专业列表、目标学校、搜索关键词

### 2.3 数据查询管线 (前端发起，后端执行)
每次用户发送报考消息时，前端自动执行：
1. 调用后端 `/api/extract` — AI 提取关键信息
2. 调用 `/recommend` — 查询本地数据库，获取冲稳保三档推荐
3. 调用 `/db_stats` — 检查该省份数据覆盖度
4. 调用多个 `/api/tavily` — 联网搜索最新录取信息
5. 将所有数据拼接为结构化的文本提示，传给 LLM

### 2.4 录取数据库查询 (后端)
- **数据源**: SQLite 数据库，包含各省份历年大学专业录取数据
- **支持两种 schema**: v1 (admission 表) 和 v2 (major_scores 表等)
- **推荐逻辑 — 冲稳保三档**:
  - 冲: 排名比用户好（min_rank < 用户排名），可以冲刺
  - 稳: 排名和用户在同一区间，有把握
  - 保: 排名比用户低，稳录取
- **渐进式回退**: 如果精确搜索无结果，逐步放宽搜索范围
  1. 精确匹配省份+科类+关键词
  2. 去掉关键词限制
  3. 扩大排名倍数（1.3x→1.5x→2.0x→3.0x）
  4. 最后按分数搜索
- **自定义数据**: 支持用户上传 Excel 表格，优先使用自定义数据

### 2.5 联网搜索 (后端)
- **主搜索**: Tavily AI Search API — 专为 AI 设计的搜索引擎
- **兜底搜索**: 百度搜索 + HTML 正则解析
- **编码修复**: Tavily 中文内容存在 GBK/Latin-1 编码问题，需要自动检测并修正
- **搜索策略**: 针对用户查询自动生成多路搜索词（验证DB数据+补充最新数据+行业趋势）
- **批量并行**: 每5个搜索词同时发送，结果去重后最多取20条

### 2.6 LLM 代理 (后端)
- **协议**: OpenAI 兼容的 chat completions API
- **流式传输**: SSE 格式，逐 chunk 返回给前端
- **特殊处理**: 某些 LLM 返回 `reasoning_content` 而非 `content`，需要兼容
- **信息提取**: 低温度调用 LLM 做结构化数据提取（省份、分数、位次等）
- **配置**: 支持环境变量和服务端默认值，也支持前端自定义

### 2.7 API 端点
| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/` | 返回 HTML 主页 |
| GET | `/health` | 健康检查，返回数据库状态 |
| GET | `/config` | 返回服务端配置 |
| GET | `/recommend` | 录取数据推荐查询 |
| GET | `/query` | 通用数据库查询 |
| GET | `/search` | 联网搜索（Tavily+Baidu） |
| GET | `/db_stats` | 数据库统计信息 |
| GET | `/userdata` | 自定义数据列表 |
| GET | `/reload_userdata` | 重新加载自定义数据 |
| POST | `/api/chat` | LLM 聊天（支持 SSE 流式） |
| POST | `/api/extract` | AI 信息提取 |
| POST | `/api/tavily` | Tavily 搜索代理 |

### 2.8 静态资源
- 主 HTML 页面（服务端读取 index.html 文件并返回）
- 两张 PNG 图片（报考模式/娱乐模式头像）
- 数据库文件（.db 或 .db.gz，自动解压）

### 2.9 Windows 部署
- 一键启动批处理文件：检查 Python 环境→安装依赖→启动服务器→启动公网隧道
- 快捷入口 HTML 文件：检测服务器状态，自动跳转

### 2.10 数据清洗脚本
- 从原始数据库清洗数据（去除无效记录）
- 从各省原始 Excel/XLS 文件重建数据库
- 支持两种数据库 schema 版本

## 3. 技术约束

- **纯 Python 标准库**: 不依赖 Flask/Django 等 Web 框架
- **零前端框架**: 原生 HTML + CSS + JavaScript
- **Windows 优先**: 批处理脚本、GBK 编码兼容
- **SQLite 数据库**: 轻量、单文件、免安装
- **外部依赖**: openpyxl (Excel 读取), xlrd (旧 Excel 格式)

## 4. 合规风险点

- **LLM Prompt 设计**: 报考模式的 system prompt 包含详细的志愿填报规则和张雪峰风格描述。这是业务逻辑，可以保留，但表述方式和结构需要重新设计。
- **百度搜索正则解析**: HTML 结构解析规则依赖百度页面格式，属于技术实现细节，不涉及合规问题。
- **省份志愿政策数据**: 各省份志愿数量和政策描述（如"安徽45个"）来自公开资料，属于事实数据。
- **娱乐模式的"张雪峰"设定**: 直接模拟特定人物的台词和口头禅可能存在版权/肖像权风险。建议改为通用的幽默风格，不绑定特定人物。

## 5. 测试标准

- 服务器启动后 `/health` 返回 ok=true
- 输入省份+分数+位次，能正确提取并查询数据库
- 输入纯中文（无数字），LLM 提取器能正确解析
- Tavily 搜索返回中文内容时，编码正确不乱码
- 数据库查询无结果时，有合理的回退逻辑
- 多轮对话能保持上下文
- 两个模式切换正常工作
- 深色/亮色模式切换正常